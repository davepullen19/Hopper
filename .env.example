# ----------------------------------------------------------------------------
# LOCAL DEVELOPMENT
# No connection pooler locally, so DATABASE_URL and DIRECT_URL are identical.
# ----------------------------------------------------------------------------
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/brewops?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/brewops?schema=public"

# ----------------------------------------------------------------------------
# PRODUCTION (Vercel + Neon)
# Neon gives you TWO connection strings:
#   DATABASE_URL -> the POOLED string (host contains "-pooler"). Used at runtime
#                   by serverless functions to avoid exhausting DB connections.
#   DIRECT_URL   -> the DIRECT (non-pooled) string. Used for `prisma migrate` /
#                   `prisma db push`.
# Example shape (replace with your real Neon values):
#   DATABASE_URL="postgresql://USER:PASS@ep-xxxx-pooler.eu-west-2.aws.neon.tech/brewops?sslmode=require"
#   DIRECT_URL="postgresql://USER:PASS@ep-xxxx.eu-west-2.aws.neon.tech/brewops?sslmode=require"
# ----------------------------------------------------------------------------
